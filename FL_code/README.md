Investigation of novel methods for gradient compression for in federated learning, leveraging distributed source coding, specifically learned wyner ziv methods.
Supported invocation from the repository root:

```bash
python -m FL_code.run_fl
```

Run experiment and debug modules the same way, for example `python -m FL_code.experiments.quantizer_check` (through FL_code so imports resolve through the `FL_code` package)
