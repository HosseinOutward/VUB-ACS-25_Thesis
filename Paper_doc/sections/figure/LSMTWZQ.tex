% Requires:
% \usepackage{tikz}
% \usetikzlibrary{arrows.meta,positioning,fit,calc}

\begin{adjustbox}{max width=\linewidth}
\begin{tikzpicture} [
  >=Latex, font=\small, transform shape,
  node distance=0.8cm and 0.7cm,
  block/.style={draw, thick, minimum width=2.0cm, minimum height=8mm, align=center},
  group/.style={draw, dotted, ultra thick, rounded corners=2mm, inner xsep=4pt, inner ysep=4pt},
  every node/.style={inner sep=1.5pt}
]

% --- main blocks (narrower/tidier) ---
\node[block] (Q)  {WZ Quantizer\\$\phi_{\theta}$};
\node[block, right=0.9cm of Q] (SWE) {Slepian--Wolf\\Encoder};
\node[block, right=0.9cm of SWE] (SWD) {Slepian--Wolf\\Decoder};
\node[block, right=0.9cm of SWD] (DQ)  {WZ Dequantizer\\$\psi_{\omega}$};

% --- input / output ---
\node[left=0.7cm of Q] (Xin) {$X$};
\draw[thick,->] (Xin) -- (Q.west);
\node[right=0.8cm of DQ] (Out) {$\hat X$};
\draw[thick,->] (DQ.east) -- (Out);

% --- pipeline arrows ---
\draw[thick,->] (Q.east)  -- (SWE.west);
\draw[thick,->] (SWD.east) -- (DQ.west);

% SWE -> SWD with labels and a small slash
\draw[thick,->] (SWE.east) -- node[midway,above=3pt] {$q_{\zeta}$}
                          node[midway,below=3pt] {$R$} (SWD.west);
\path (SWE.east) -- (SWD.west) coordinate[pos=.5] (midRS);
\draw[thick] ($(midRS)+(-0.11,0.18)$) -- ($(midRS)+(0.11,-0.18)$);

% --- side information Y with clean right-angle branches ---
\coordinate (Y)   at ($(SWD.south) + ( -0.0,-0.9)$);
\coordinate (YJ)  at ($(SWD.south) + (  0.0,-0.4)$); % junction under SWD
\draw[thick] (Y) node[below=3pt] {$Y$} -- (YJ);
\draw[thick,->] (YJ) -- (SWD.south);
\draw[thick,->] (YJ) -| (DQ.south);

% --- dotted grouped regions ---
%\node[group, fit=(Q)] (GQ) {};
%\node[group, fit=(SWE)(SWD)] (Gmid) {};
%\node[group, fit=(DQ)] (GDQ) {};

% --- "ANN" labels placed well above the groups (no overlap) ---
%\node[anchor=south] at ($(GQ.north)+(0,4mm)$) {ANN};
%\node[anchor=south] at ($(GDQ.north)+(0,4mm)$) {ANN};

\end{tikzpicture}
\end{adjustbox}
