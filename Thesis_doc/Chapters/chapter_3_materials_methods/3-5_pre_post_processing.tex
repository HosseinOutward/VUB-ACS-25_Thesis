\section{Pre/Post Processing}
During our initial experiments, we observed a significant drop in training convergence speed when applying our WZ quantization.
We will propose 2 design choices to rectify this and investigate their effectivness: (i) per‑layer normalization (e.g., 99th‑percentile scaling) keeps dynamic ranges comparable across layers and rounds; and (ii) more active outlier handling to reduce the impact of extreme values on training.

\subsection{Normalization and Per‑Layer Distribution}
If we consider the layer by layer nature of feedforward neural networks, we can intuitivly assume that any distortion introduced in one layer will be propagated and potentially amplified in subsequent layers. This distortion should be considered as relative to the weights of that layers and not the entire model, as each layer has its own distribution of weights and gradients.
Now if we look at the distribution of weights per layer, as in figure \ref{fig:custom_cnn_weight_histograms}, we notice that they can vary significantly in their dynamic range, and remembering the our WZ quantizer is sensetive to the dynamic range of the input, as explored in section \ref{sec:??}, we can assume that it might be benificial to normalize the weights per layer to a common dynamic range.

Unfortunately, we did not have time to implement the normlization to consider the model weights for calculating the distortion per layer, and instead used the gradients per layer for the normalization. This might not be ideal as gradients in one layer can be very small relative to the weights they are updating, and thus have small effect on future layers. However, one could argue that the average of all gradients of a layer are a good proxy for the weights of that layer, as they are calculated based on the loss function and the weights of that layer.

for reconstruction , we must send the normlization parameters along with the gradients to the server. To keep the overhead to a minimum, we use a simple normalization scheme, where we scale the weights of each layer to the 99th percentile of their absolute values. if we use the true max value, outliers will skew the range, which beats the purpose of normalization. with these method, we only need to send one number per layer, the 99th percentile value, which means an overhead of 41 extra numbers for a ResNet-18, which is negligible compared to the number of weights. The normalization is done as follows:
\equation
    W_i' = \frac{W_i}{P_{99}(W_i)}
\endequation
where \(W_i\) are the weights of layer \(i\), \(P_{99}(W_i)\) is the 99th percentile of the absolute values of \(W_i\), and \(W_i'\) are the normalized weights. The denormalization is done as follows:
\equation
    W_i = W_i' \times P_{99}(W_i)
\endequation
where \(W_i\) are the denormalized weights, \(W_i'\) are the normalized weights, and \(P_{99}(W_i)\) is the 99th percentile of the absolute values of \(W_i\). This method also keeps the center of the distribution intact, which is usually around zero.

its important to note that this normalization might not be needed depndeing on the model and dataset. for example, we looked into the gradients of a ResNet-18 trained on SVHN on figure \ref{fig:resnet18_weight_histograms}, and found that they were all in a similar range, making normalization unnecesary. However, when looking at a custom CNN gradients trained on SVHN on figure \ref{fig:custom_cnn_weight_histograms}, we found that the gradients varied significantly in their dynamic range per layer, making layer-wise normalization benificial.

We tried to evaluate if there are any unforseen gains from this normlization on Resnet18 when used for Federated learning, and found that the convergence speed was similar in both cases.
We did not have time to fully evalueate the effectivness of this normalization on the custom CNN, but we did notice a significant increase in convergence in the initial rounds

\figure[H]
    \centering
    \includegraphics[width=0.8\textwidth]{Chapters/chapter_3_materials_methods/figures/histograms for normlization - custom cnn}
    \caption{Weight histograms of a custom CNN trained on SVHN, by foucsing on the range of values, we see that the layers have different dynamic ranges, making normalization benificial.}
    \label{fig:custom_cnn_weight_histograms}
\endfigure

\figure[H]
    \centering
    \includegraphics[width=0.8\textwidth]{Chapters/chapter_3_materials_methods/figures/histograms for normlization - custom cnn}
    \caption{Weight histograms of a custom CNN trained on SVHN, by foucsing on the range of values, we see that the layers have similar dynamic ranges, making normalization unnecesary.}
    \label{fig:resnet18_weight_histograms}
\endfigure

\subsection{Outlier Handling}
We use SGD optimizer which means our gradients are directly propotional to the change in the final loss function. This means that very large gradients, have as large impact on the loss function, and thus the training process. However, these large gradients are often treated as outliers and not reconstructed well by our WZ quantizer. As we saw in section \ref{sec:??}, foucses on regions with high density. one could argue that the quantizer is minimizing the reconstruction loss, and thus its doing the best job possible, but considering the very few number of outliers, we could add an additional mechanism to handle them separately and improve the overall reconstruction quality without increasing the bitrate too much.
We propose making a seperate gradient vectors which only includes the outliers. its important to not remove the outliers from the original gradient vector as they will be needed to find the positions of the outlier during reconstruction. We found that values 30\% beyond the 99th percentile mark are usually not reconstructed well, as seen in the figure \ref{fig:outlier_effect}, but are also very few in number, around 12k for resnet18 on SVHN relative to more than 11 Milion gradient values (less than 0.1\%).
The original gradient vector is quantized normally using our WZ quantizer. For the seperated values, there will be a gap between the negative and positive values caused by removing the normal values. we will remove this gap by shifting the outliers towards zero, and then we will treat them as a seperate layer of the model, normlizing them and quantizing them using our WZ quantizer. later when reconstructing, we will reverse this gap removal. this does mean that we need to send the parameters relating to the normlization and size of the gap, which is usally only a few number. Its important to also send the sign of the outliers, as the majority will be mapped to zero and loss their sign. the overhead for these signs is also minimal, usually less than a KB, which is negligible compared to the size of the gradients which is in the MBs.
The hard part is sending over the position of the outlier in the gradient vector thats been quantized. sending their index is inefficient, as these indexes usually have a high bit rate as they are big numbers due to the large size of the gradient vector. instead we modify the bin vector which was made by quantizing the original gradient vector, with the outliers intact. we set the bins which correspond to the outliers to number of bins + 1. This will cause a few issues with the next step of DSC coding which we will address in \ref{sec:??}.

In the figure \ref{fig:outlier_effect_on_convergence}, we can see the effect of outlier handling on the covergence speed of the Resnet 18 on SVHN. we can see that the outlier handling significantly improves the convergence speed and makes it comparable to the unquantized model.

\figure[H]
    \centering
    \includegraphics[width=0.8\textwidth]{Chapters/chapter_3_materials_methods/figures/outlier_effect.png}
    \caption{Effect of outliers on reconstruction quality, we can see that the outliers are not reconstructed well, and thus have a high reconstruction error.}
    \label{fig:outlier_effect}
\endfigure
